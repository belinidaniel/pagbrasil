<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Validation_Not_Edit_Sales</fullName>
    <active>false</active>
    <description>Sales NB and Sales XB can&apos;t edit Type field in Account</description>
    <errorConditionFormula>AND(
    OR(
        $Profile.Name = &quot;Sales NB&quot;,
        $Profile.Name = &quot;Sales XB&quot;
    ),
    ISCHANGED(Type),
    NOT(ISNEW())
)</errorConditionFormula>
    <errorMessage>Sales NB and Sales XB can&apos;t edit Type field in Account</errorMessage>
</ValidationRule>
